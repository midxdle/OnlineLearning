extends base

block content
  link(href='/stylesheets/home.css', rel='stylesheet')
  h1 صفحه اصلی
  div(class="main")
    div(class="course")
      if courses
        each course in courses
          div(class="courseItems")
            img(src=`${course.image}`)
            h4 دوره آموزشی #{course.name}
            p #{course.details}
            div قیمت دوره : 
              span #{course.price}
    div(class="content")
      if contents
        each content in contents
          a(href=`/content/${content._id}`) بیشتر...